import Personnages from './classes/personnages'

export default class Mechant extends Personnages {

  //Constructeur
  constructor(nom = "", pv = 0, pvMax = 0, def = 0) {
    super(nom, pv, pvMax, def);
  }

  randomName = () => {
    // TODO: randomName function
  }
}